import { TRADE_FEE_LABEL } from './chainConfig';

export const LAUNCH_SUMMARY =
  'Rex hosts your coin, routes trade tax into a non-custodial marketing wallet, and pays approved suppliers for growth.';

export const LAUNCH_NOTE =
  `Trades on Rex feed your marketing wallet. ${TRADE_FEE_LABEL} on each swap — kept low so bot volume still fills the curve.`;

export const LAUNCH_TRADE_NOTE =
  'Every buy and sell takes 0.9% total: 0.35% Rex protocol + 0.15% creator vault (withdrawable) + 0.40% marketing wallet PDA.';
